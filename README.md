# 💜 Pantha Terminal

Pantha Terminal is an aesthetic neon-purple terminal UI built using **Textual**.

## ✨ Features
- Purple glowing borders (animated pulse)
- Command input prompt: `Pantha >`
- Built-in commands (`help`, `clear`, `ls`, `pwd`, etc.)
- GitHub Actions CI using `build.yml`
- Includes `assets/icon.ico` for packaging

## 🚀 Run locally

```bash
pip install -r requirements.txt
python -m app.main
